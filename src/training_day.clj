(ns training-day)

(def answer 42)

(def hai "O HAI!")

(defn square [x]
  "Takes x and multiplies it by itself"
  (* x x))

(defn average [a b]
  "Returns the average of a and b as a ratio"
  (/ (+ a b) 2))
