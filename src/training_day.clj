(ns training-day)

(def answer 42)

(def hai "O HAI!")

(defn square [x]
  "Takes a number and multiplies it with itself"
  (* x x))

(defn average [a b]
  "Returns the average of two parameters"
  (/ (+ a b) 2))
