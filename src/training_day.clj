(ns training-day)

(def answer 42)

(def hai "O HAI!")

(defn
  square
  "Multiplies parameter with itself."
  [x]
  (* x x))

(defn
  average
  "Returns the average of its two parameters"
  [a b]
  (/ (+ a b) 2))
